spring:
  datasource:
    url: jdbc:h2:mem:it-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  cache:
    type: none
security:
  jwt:
    secret: test-secret-at-least-thirty-two-bytes-long
    issuer: clean-it-test
    audience: clean-it-api
    jwk-set-uri: ""
  basic:
    enabled: false
stripe:
  secret-key: sk_test_not_used
  publishable-key: pk_test_not_used
  webhook-secret: whsec_test
