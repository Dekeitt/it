spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  data:
    redis:
      host: ${SPRING_REDIS_HOST}
      port: ${SPRING_REDIS_PORT:6379}
      password: ${SPRING_REDIS_PASSWORD:}
  mail:
    host: ${SMTP_HOST:localhost}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USERNAME:}
    password: ${SMTP_PASSWORD:}
    properties:
      mail.smtp.auth: ${SMTP_AUTH:true}
      mail.smtp.starttls.enable: ${SMTP_STARTTLS:true}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    validate-migration-naming: true

security:
  jwt:
    secret: ${JWT_SECRET:}
    issuer: ${JWT_ISSUER}
    audience: ${JWT_AUDIENCE}
    jwk-set-uri: ${JWT_JWK_SET_URI:}
  basic:
    enabled: false

stripe:
  secret-key: ${STRIPE_SECRET_KEY}
  publishable-key: ${STRIPE_PUBLISHABLE_KEY}
  webhook-secret: ${STRIPE_WEBHOOK_SECRET}
  connect-webhook-secret: ${STRIPE_CONNECT_WEBHOOK_SECRET:}

marketplace:
  connect:
    enabled: ${CONNECT_ENABLED:false}
    platform-fee-bps: ${CONNECT_PLATFORM_FEE_BPS:0}
    allowed-countries: ${CONNECT_ALLOWED_COUNTRIES:}
    refund-reverse-transfer: ${CONNECT_REFUND_REVERSE_TRANSFER:unset}
    refund-application-fee: ${CONNECT_REFUND_APPLICATION_FEE:unset}
    on-behalf-of: ${CONNECT_ON_BEHALF_OF:false}
    refresh-url: ${CONNECT_REFRESH_URL:}
    return-url: ${CONNECT_RETURN_URL:}

notifications:
  worker:
    enabled: ${NOTIFICATIONS_WORKER_ENABLED:true}
    delay-ms: ${NOTIFICATIONS_WORKER_DELAY_MS:5000}
  reminders:
    enabled: ${NOTIFICATIONS_REMINDERS_ENABLED:true}
    delay-ms: ${NOTIFICATIONS_REMINDERS_DELAY_MS:3600000}
  email:
    enabled: ${NOTIFICATIONS_EMAIL_ENABLED:false}
    from: ${NOTIFICATIONS_EMAIL_FROM:no-reply@cleanit.es}
  push:
    enabled: ${NOTIFICATIONS_PUSH_ENABLED:false}
    public-key: ${VAPID_PUBLIC_KEY:}
    private-key: ${VAPID_PRIVATE_KEY:}
    subject: ${VAPID_SUBJECT:mailto:admin@cleanit.es}
